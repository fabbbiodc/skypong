{
  "name": "skypong-monorepo",
  "private": true,
  "workspaces": [
    "common",
    "client"
  ],
  "scripts": {
    "dev": "cd client && vite",
    "build": "cd client && tsc && vite build"
  }
}
